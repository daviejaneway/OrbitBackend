api Main
    type Main(argc Int32, argv [[Int8]])
    trait A(x Int, y Int)

    type B(x Int, y Int) : A

    (A) foo (a A) ()
        debug a.x
    ...

    (self Main) main () (Int32)
        b = B(1, 2)
        A.foo(b)

        return self.argc
    ...
...
